---
id: "2026-08-10-合并上游 75e73f3"
title: "合并上游 75e73f3"
status: "in_progress"
created: "2026-08-10"
updated: "2026-08-10"
category: "架构"
tags: ["workhub", "upstream-sync", "75e73f3", "static-verification"]
---

# Issue: 合并上游 75e73f3

## Goal

在不触碰主工作树未提交改动、不执行任何 Cargo 命令的前提下，将 `upstream/main` 的 `75e73f3d6ac0350d211f12ae7d57c2c0aad72576` 安全集成到隔离分支，并用静态证据证明冲突已解析、Pi-Grok 架构接缝未被意外覆盖。

## 背景/问题

当前本地已同步上游基线为 `a5589e958437d79e13db026eedcb1720bffd4063`，本地 `main` HEAD 为 `af668c6f15ead7200f2e7ae3eaf04bc71117c64a`。待同步范围包含 4 个上游提交、406 个文件、`+36871/-12418`，新 `SOURCE_REV` 为 `a61c32b12a2b400f212221cd8762e05f9b36828d`。

主工作树存在未提交主题/ACP 改动和大量未跟踪 Pi theme 文件，本轮只在 `/Users/dengwenyu/Dev/AI/pi-grok-build-sync-75e73f3` 的 `sync/upstream-75e73f3` 分支执行。静态交集分析显示 89 个上游文件也在本地基线后被修改；`git merge-tree --write-tree HEAD upstream/main` 预演得到 18 个内容冲突。

## 验收标准 (Acceptance Criteria)

- [x] `docs/upstream/UPSTREAM_CHANGELOG.md` 在真实 merge 前完整记录 `a5589e9..75e73f3` 的权威 `Changes:`、统计和合并风险。
- [x] 已建立隔离 worktree/分支，起点精确为 `af668c6f`；主工作树未提交改动不参与本分支。
- [x] 已完成静态三方预演，确认 18 个内容冲突与 89 个本地/上游文件交集。
- [ ] 真实 merge 逐文件解析全部冲突，并审查 stage 1/2/3；`git ls-files -u` 最终为空。
- [ ] Pi 继续拥有 agent/session/queue 语义；Pager 继续是唯一 TUI；`pi-grok-adapter` 继续 headless、library-only。
- [ ] 不修改 `pi-main` gitlink，不使用上游 Shell/ACP session 语义替代 Pi session owner。
- [ ] 更新 `SOURCE_REV` 与 `AGENTS.md` base 为本轮上游事实；verifier allowlist 仅在确有新窄接缝且经静态审查时才允许变化。
- [ ] 全程不执行 `cargo`、`./build.sh`、`./verify.sh` 或任何间接触发 Cargo 的命令。
- [ ] 静态验证至少包括：无冲突标记、`git diff --check`、merge parents、上游 tip 可达、SOURCE_REV/base/pi-main gitlink、关键接缝 diff 和可安全运行的非 Cargo verifier/脚本。
- [ ] 最终回写 Result/Verification，明确静态验证的能力边界；不 push 远端。

## 预演冲突

18 个内容冲突：

- Pager ACP: `src/app/acp_handler/interactions.rs`, `src/app/acp_handler/mod.rs`。
- Pager dispatch: `router.rs`, `settings/ui.rs`, `status.rs`, `task_result.rs`。
- Pager app root: `event_loop.rs`, `mod.rs`, `modals.rs`。
- Scrollback/slash: `scrollback/blocks/context_info.rs`, `scrollback/state/mod.rs`, `slash/acp_command.rs`, `slash/commands/mod.rs`。
- Views: `views/modal.rs`, `views/rewind.rs`, `views/welcome/mod.rs`。
- Shell workflow: `session/workflow/host_service.rs`, `session/workflow/manager.rs`。

## 关键决策

| 决策 | 理由 |
|---|---|
| 上游核心结构优先，Pi-Grok 接缝外科重放 | 降低未来上游同步冲突，同时保持产品边界 |
| 仅在隔离 worktree 合并 | 主工作树有用户未提交改动，隔离是数据安全前提 |
| 每个冲突比较 stage 1/2/3 后解析 | 防止简单 ours/theirs 丢失上游新语义或本地窄接缝 |
| Pi session/queue/agent ownership 不随上游 Shell/ACP 演进迁移 | 这是项目架构不变量 |
| 禁止 Cargo，以静态证据闭环 | 用户明确要求；最终不能声称编译/测试通过 |
| 不扩大 verifier allowlist 来掩盖冲突 | 新接缝必须显式、有事实依据地审查 |

## 回滚计划

- merge 未提交前：在隔离 worktree 执行 `git merge --abort`。
- merge 提交后：保留隔离分支作为候选；如需放弃，移除 worktree/分支即可，不影响 `main`。
- 不使用 `reset --hard`、广域 restore、force checkout 或 push。
- 主工作树未提交文件始终留在原 worktree，不复制到同步分支。

## 实施阶段

### Phase 1: 规划和准备
- [x] fetch upstream 并核对 commit range / SOURCE_REV / diff stats
- [x] 分析本地/上游交集和 merge-tree 冲突
- [x] 建立隔离 worktree/branch 和回滚边界
- [x] 完成本轮 upstream changelog 记录并提交规划文档

### Phase 2: 合并和冲突解析
- [ ] merge `75e73f3`（先 `--no-commit`）
- [ ] 逐文件读取 stage 1/2/3，吸收上游结构并保留 Pi-Grok 窄接缝
- [ ] 审查自动合并的高风险 Pager/session/queue/external-profile 文件
- [ ] 提交两亲 merge commit

### Phase 3: 静态验证
- [ ] 无 unmerged entries / conflict markers / whitespace errors
- [ ] merge parents、upstream ancestry、SOURCE_REV、AGENTS base、pi-main gitlink 正确
- [ ] 审查 Pi-Grok 架构关键字、source-identity/verifier 接缝与最终 diff 范围
- [ ] 运行所有可安全执行且不触发 Cargo 的项目静态 verifier/脚本

### Phase 4: 交付
- [ ] 回写 Result/Verification 和已知验证边界
- [ ] 保留隔离分支，不 push

## 遇到的错误

| 日期 | 错误 | 解决方案 |
|---|---|---|
| 2026-08-10 | 首次 worktree 创建后并行读取发生竞态，读取方短暂报告目录不存在 | 随后串行复核 worktree 已创建且 HEAD/branch 正确；未对仓库造成额外修改 |

## Notes

- 上游 tip: `75e73f3d6ac0350d211f12ae7d57c2c0aad72576`。
- 新 `SOURCE_REV`: `a61c32b12a2b400f212221cd8762e05f9b36828d`。
- 静态预演输出: `/tmp/pi-grok-merge-tree-75e73f3.txt`。
- 主工作树保持在 `main@af668c6f`，其未提交改动不属于本次同步候选。

---

## Status 更新日志

- **2026-08-10**: 状态变更 → in_progress，备注: 完成 fetch、范围统计、隔离 worktree 与 merge-tree 预演；禁止 Cargo。