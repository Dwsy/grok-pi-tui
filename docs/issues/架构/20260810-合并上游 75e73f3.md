---
id: "2026-08-10-合并上游 75e73f3"
title: "合并上游 75e73f3"
status: "completed"
created: "2026-08-10"
updated: "2026-08-10"
category: "架构"
tags: ["workhub", "upstream-sync", "75e73f3", "static-verification"]
---

# Issue: 合并上游 75e73f3

## Goal

在不触碰主工作树未提交改动、不执行任何 Cargo 命令的前提下，将 `upstream/main` 的 `75e73f3d6ac0350d211f12ae7d57c2c0aad72576` 安全集成到隔离分支，并用静态证据证明冲突已解析、Pi-Grok 架构接缝未被意外覆盖。

## 背景/问题

当前本地已同步上游基线为 `a5589e958437d79e13db026eedcb1720bffd4063`，本地 `main` HEAD 为 `af668c6f15ead7200f2e7ae3eaf04bc71117c64a`。待同步范围包含 4 个上游提交、406 个文件、`+36871/-12418`，新 `SOURCE_REV` 为 `a61c32b12a2b400f212221cd8762e05f9b36828d`。

主工作树存在未提交主题/ACP 改动和大量未跟踪 Pi theme 文件，本轮只在 `/Users/dengwenyu/Dev/AI/pi-grok-build-sync-75e73f3` 的 `sync/upstream-75e73f3` 分支执行。静态交集分析显示 89 个上游文件也在本地基线后被修改；`git merge-tree --write-tree HEAD upstream/main` 预演得到 18 个内容冲突。

## 验收标准 (Acceptance Criteria)

- [x] `docs/upstream/UPSTREAM_CHANGELOG.md` 在真实 merge 前完整记录 `a5589e9..75e73f3` 的权威 `Changes:`、统计和合并风险。
- [x] 已建立隔离 worktree/分支，起点精确为 `af668c6f`；主工作树未提交改动不参与本分支。
- [x] 已完成静态三方预演，确认 18 个内容冲突与 89 个本地/上游文件交集。
- [x] 真实 merge 逐文件解析全部冲突，并审查 stage 1/2/3；`git ls-files -u` 最终为空。
- [x] Pi 继续拥有 agent/session/queue 语义；Pager 继续是唯一 TUI；`pi-grok-adapter` 继续 headless、library-only。
- [x] 不修改 `pi-main` gitlink，不使用上游 Shell/ACP session 语义替代 Pi session owner。
- [x] 更新 `SOURCE_REV` 与 `AGENTS.md` base 为本轮上游事实；未扩大 verifier allowlist。
- [x] 全程不执行 `cargo`、`./build.sh`、`./verify.sh` 或任何间接触发 Cargo 的命令。
- [x] 静态验证包括：无冲突标记、`git diff --check`、merge parents、上游 tip 可达、SOURCE_REV/base/pi-main gitlink、关键接缝 diff 和可安全运行的非 Cargo verifier/脚本。
- [x] 最终回写 Result/Verification，明确静态验证的能力边界；不 push 远端。

## 预演冲突

18 个内容冲突：

- Pager ACP: `src/app/acp_handler/interactions.rs`, `src/app/acp_handler/mod.rs`。
- Pager dispatch: `router.rs`, `settings/ui.rs`, `status.rs`, `task_result.rs`。
- Pager app root: `event_loop.rs`, `mod.rs`, `modals.rs`。
- Scrollback/slash: `scrollback/blocks/context_info.rs`, `scrollback/state/mod.rs`, `slash/acp_command.rs`, `slash/commands/mod.rs`。
- Views: `views/modal.rs`, `views/rewind.rs`, `views/welcome/mod.rs`。
- Shell workflow: `session/workflow/host_service.rs`, `session/workflow/manager.rs`。

## 关键决策

| 决策 | 理由 |
|---|---|
| 上游核心结构优先，Pi-Grok 接缝外科重放 | 降低未来上游同步冲突，同时保持产品边界 |
| 仅在隔离 worktree 合并 | 主工作树有用户未提交改动，隔离是数据安全前提 |
| 每个冲突比较 stage 1/2/3 后解析 | 防止简单 ours/theirs 丢失上游新语义或本地窄接缝 |
| Pi session/queue/agent ownership 不随上游 Shell/ACP 演进迁移 | 这是项目架构不变量 |
| 禁止 Cargo，以静态证据闭环 | 用户明确要求；最终不能声称编译/测试通过 |
| 不扩大 verifier allowlist 来掩盖冲突 | 新接缝必须显式、有事实依据地审查 |

## 回滚计划

- merge 未提交前：在隔离 worktree 执行 `git merge --abort`。
- merge 提交后：保留隔离分支作为候选；如需放弃，移除 worktree/分支即可，不影响 `main`。
- 不使用 `reset --hard`、广域 restore、force checkout 或 push。
- 主工作树未提交文件始终留在原 worktree，不复制到同步分支。

## 实施阶段

### Phase 1: 规划和准备
- [x] fetch upstream 并核对 commit range / SOURCE_REV / diff stats
- [x] 分析本地/上游交集和 merge-tree 冲突
- [x] 建立隔离 worktree/branch 和回滚边界
- [x] 完成本轮 upstream changelog 记录并提交规划文档

### Phase 2: 合并和冲突解析
- [x] merge `75e73f3`（先 `--no-commit`）
- [x] 逐文件读取 stage 1/2/3，吸收上游结构并保留 Pi-Grok 窄接缝
- [x] 审查高风险 Pager/session/queue/external-profile 接缝与最终索引树范围
- [x] 提交两亲 merge commit `dc690ac2139cd62bf7c44400da704e3dc5ff52b9`

### Phase 3: 静态验证
- [x] 无 unmerged entries / conflict markers / whitespace errors
- [x] merge parents、upstream ancestry、SOURCE_REV、AGENTS base、pi-main gitlink 正确
- [x] 审查 Pi-Grok 架构关键字、source-identity/verifier 接缝与最终 diff 范围
- [x] 运行所有可安全执行且不触发 Cargo 的项目静态 verifier/脚本；环境/既有基线阻塞项记录如下

### Phase 4: 交付
- [x] 回写 Result/Verification 和已知验证边界
- [x] 保留隔离分支，不 push

## Result

- 上游 `75e73f3d6ac0350d211f12ae7d57c2c0aad72576` 已在隔离分支 `sync/upstream-75e73f3` 合并为双亲提交 `dc690ac2139cd62bf7c44400da704e3dc5ff52b9`；父提交为规划审计点 `982999c4f9e253ce971914c5ae796d7b724b4fdb` 与上游 tip。
- 18 个实际内容冲突与静态预演完全一致，全部逐文件解析；最终 `git ls-files -u` 为空，无冲突 marker。
- `SOURCE_REV` 已更新为 `a61c32b12a2b400f212221cd8762e05f9b36828d`，`AGENTS.md` base 已更新为 `75e73f3d6ac0350d211f12ae7d57c2c0aad72576`。
- `pi-main` gitlink 保持 `4f0437e2d58d651dd934119ecabea2893975f62f`，未变化。
- 最终索引树相对 upstream 的差异路径全部来自合并前已有本地改动：`final_paths_not_preexisting_local = []`；18 个冲突文件全部同时属于本地历史改动与本轮 upstream 改动。
- 未扩大 `grok_uploaded_baseline_sha256.json` / renderer verifier allowlist，避免用宽松规则掩盖既有 source-identity 漂移。
- 未 push；主工作树 `main` 的用户未提交改动未被触碰。

## Verification

### PASS

- `git diff --cached --check`：提交前通过。
- `git ls-files -u`：0；人工解析文件无 `<<<<<<<` / `=======` / `>>>>>>>` marker。
- `rustfmt --edition 2024 --config skip_children=true --check`：18 个冲突解析 Rust 文件全部通过解析/格式检查；该命令不调用 Cargo。
- Merge commit：`dc690ac2` 有 2 个 parent，且 `upstream/main` 是其 ancestor。
- 关键静态 seam 断言通过：external startup + upstream startup telemetry、Pi cache-graph/上游 UsageInfo 分流、Pi direct slash command + upstream `SkillMeta`、workflow backend ownership、rewind `ListOverlay` + 文件快照计数、welcome 去 Beta + external brand。
- `verify_native_grok.py` 中 adapter library-only、无 terminal dependency/renderer loop、native Pager composition、native TUI components、声明 seam 精确性等架构检查通过。

### 已知阻塞 / 非本轮回归

- `check_rust_syntax.py` 未运行到语法扫描：环境缺少 Python `tree_sitter` 模块。作为替代，本轮人工冲突 Rust 文件已全部由 `rustfmt --check` 成功解析。
- `mock_pi_contract.py` 因可选 `pi-main` checkout 未 provision（缺 `packages/coding-agent/src/modes/rpc/rpc-types.ts`）而停止；未执行 Cargo。
- `verify_native_grok.py` 的完整 source-identity/renderer hash 检查失败，但对合并前 `HEAD=982999c` 用同一现有 baseline 做纯 Git/hash 复核时已经存在 `35 missing / 1173 mismatch / 702 extra`，证明这是本轮 merge 前的基线漂移。故本轮没有通过扩大 allowlist 或刷新无关基线来伪造 PASS。
- 用户明确禁止 Cargo，因此没有执行编译、Cargo check/test、`./build.sh` 或 `./verify.sh`；不能声称编译/运行时测试通过。

## 遇到的错误

| 日期 | 错误 | 解决方案 |
|---|---|---|
| 2026-08-10 | 首次 worktree 创建后并行读取发生竞态，读取方短暂报告目录不存在 | 随后串行复核 worktree 已创建且 HEAD/branch 正确；未对仓库造成额外修改 |
| 2026-08-10 | 并行 Git 静态检查短暂争用 worktree `index.lock` | 改为串行 Git 检查；锁自然释放，无需删除锁文件，最终 `write-tree`/commit 正常 |
| 2026-08-10 | `rustfmt --check` 暴露 `modals.rs` 两处缺失闭合括号与 `rewind.rs` closure 结构错误 | 回到上游 `ListOverlay` 结构重放 Pi seam；修复后 18 个冲突文件均通过 rustfmt parser/check |
| 2026-08-10 | tree-sitter verifier 缺 Python 模块；mock verifier 缺 provisioned `pi-main` 源 | 记录为环境阻塞，不安装依赖、不修改 Pi gitlink；使用 rustfmt/static Git 证据补足本轮可执行验证 |

## Notes

- 上游 tip: `75e73f3d6ac0350d211f12ae7d57c2c0aad72576`。
- 新 `SOURCE_REV`: `a61c32b12a2b400f212221cd8762e05f9b36828d`。
- 静态预演输出: `/tmp/pi-grok-merge-tree-75e73f3.txt`。
- 主工作树保持在 `main@af668c6f`，其未提交改动不属于本次同步候选。

---

## Status 更新日志

- **2026-08-10**: 状态变更 → in_progress，备注: 完成 fetch、范围统计、隔离 worktree 与 merge-tree 预演；禁止 Cargo。
- **2026-08-10**: 状态变更 → completed，备注: `dc690ac2` 双亲 merge 完成；18 个冲突清零，静态验证闭环；Cargo 明确未运行。