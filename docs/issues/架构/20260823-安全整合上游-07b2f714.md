---
id: "2026-08-23-安全整合上游-07b2f714"
title: "安全整合上游 07b2f714"
status: "active"
created: "2026-08-23"
updated: "2026-08-23"
category: "架构"
tags: ["upstream", "pager", "pi-grok", "sync", "workflow", "subagent", "mcp"]
---

# Issue: 安全整合上游 07b2f714

## Goal

在不破坏 grok-pi 既有功能和架构边界的前提下，将 upstream `07b2f7144fd5c5c9d3dd1966937a87852d2dbdb8` 集成到本地 `main`。对于结构差异大的区域，以“上游结构优先 + 最小 Pi seam 重放”为原则：吸收上游核心实现，不把 Pi-owned agent/session/queue/tool/extension 语义移交给 stock Grok Shell。

上游事实记录：[`docs/upstream/UPSTREAM_CHANGELOG.md`](../../upstream/UPSTREAM_CHANGELOG.md) `[07b2f714]`。

## Complexity / baseline

- **Complexity:** L4（1002 文件；+110382/−26245；8 个上游提交；173 个双边修改文件；静态预览 49 个冲突）。
- **Fork baseline:** `46f6f31b194a85ce62e9b12438708a52bcb8f2e8`（记录前）。
- **Last synced upstream:** `e5fd4816d43260c15ba785f103990c1ed6cea230`。
- **Target upstream:** `07b2f7144fd5c5c9d3dd1966937a87852d2dbdb8`。
- **SOURCE_REV target:** `956313d459bee15ae8f17bf73e0633605e18dddd`。
- **Delivery policy:** isolated worktree, two-parent merge, verified ff-only back to local `main`; no push/rebase/force-update/destructive restore.
- **Existing untracked work:** `.pi/gapp/` stays untouched.

## Architecture invariants

1. Grok Pager remains the only visible TUI; no second Pi TUI.
2. Pi remains the only Agent Core and owns provider/model registry, sessions, queue/interject semantics, trust, tools, extensions, compaction and retry semantics exposed by grok-pi.
3. `pi-grok-adapter` remains headless/library-only; do not expand Pi source/RPC by modifying `pi-main`.
4. Prefer upstream native Pager structure/components, then reapply fork seams surgically at external-profile/ACP boundaries.
5. Product state remains isolated at `~/.grok-pi` / `<repo>/.grok-pi`; `project_config_dir()` and `$GROK_HOME` routing must not regress.
6. Existing queue semantics (editable/removable held steer/follow-up rows, read-only external lane), Pi session tree/fork/clone/reload, Remote TUI and native conflict gating remain intact.
7. Existing Pi workflow seam remains `xai-workflow` + `ExternalWorkflowRuntime` + adapter host; upstream workflow UI/engine improvements may be absorbed without changing Pi spawn ownership.
8. Existing Pi subagent V1/V2 semantics and product-isolated agent/team definitions remain intact; upstream concurrency/retry/render improvements may be adopted behind that seam.
9. The new upstream in-process screen-mode switch is adopted only if it preserves the same external-profile process, Pi argv/session and prompt/queue state; old re-exec behavior stays excluded.
10. `pi-main` gitlink and source-identity verification are not weakened or silently advanced.

## Risk map

| Area | Risk | Resolution principle |
|---|---|---|
| Pager app/dispatch/event loop | Critical | take upstream control-flow structure; reinsert external-profile actions/routes only where required |
| Workflow | Critical | keep Pi spawn/backend ownership; absorb upstream engine/UI changes around `ExternalWorkflowRuntime` |
| Screen mode | Critical | preserve Pi process/argv/session; validate in-process round trip before exposing commands |
| Product paths/config | Critical | preserve `.grok-pi` routing while incorporating upstream shared-home/config improvements |
| Queue/interject/prompt | High | Pi queue remains source of truth; adopt native rendering/interaction fixes without changing delivery semantics |
| Subagents/dashboard | High | preserve Pi child lifecycle/team semantics; absorb non-owning Pager visualization/retry improvements |
| MCP/elicitation | High | avoid stock product MCP ownership; reuse native elicitation/UI where adapter/session boundary permits |
| Permission/settings | High | preserve grok-pi F2/native feature policy and project trust boundary while absorbing upstream safety defaults |
| Pager render/selection | Medium | upstream-first unless it conflicts with grok-pi diff/user-markdown surfaces |
| Shell auth/worktree/telemetry | Medium | keep only behavior that does not become a second agent/session authority |

## Acceptance criteria

- [x] All 8 upstream commits are ancestors of the integration tip; two-parent history is preserved.
- [x] All 49 predicted conflicts are resolved by explicit stage-1/2/3 review; no blanket ours/theirs on seam files.
- [ ] `pi-grok-adapter` remains headless and Pi-owned queue/session/model/tool/extension semantics pass their tests.
- [ ] grok-pi startup flags, Remote TUI, product path isolation, session resume/tree/fork/clone/reload, queue/interject, background Bash and native extension UI remain functional.
- [ ] Workflow and subagent V1/V2 semantics remain Pi-backed; upstream UI/concurrency improvements do not instantiate stock Grok agent ownership.
- [ ] In-process minimal/fullscreen switching either passes external-profile state-preservation tests or stays hidden for grok-pi.
- [x] `SOURCE_REV` and `AGENTS.md` base update only after coherent merge and point exactly at the target.
- [x] `pi-main` gitlink is unchanged.
- [ ] `git diff --check`, adapter tests, grok-pi bin check/tests and `./build.sh` are run and read; broader verifier blockers are separated from regressions.
- [ ] Local `main` receives only the verified integration tip via ff-only; `.pi/gapp/` remains untouched; no remote push.

## Plan

### Phase 1 — Record / baseline
- [x] Fetch upstream and record the 8 authoritative `Changes:` lists.
- [x] Compute range, area stats and static merge preview.
- [x] Create this L4 issue and acceptance criteria.
- [ ] Commit documentation-only pre-merge record and create backup ref.
- [ ] Capture focused pre-merge verification baseline.

### Phase 2 — Isolated merge
- [x] Create `sync/upstream-07b2f714` in an isolated worktree.
- [x] Merge `upstream/main` with `--no-ff --no-commit`.
- [x] Inventory all conflicts and inspect merge stages before resolution.
- [x] Resolve upstream-first, then replay only required grok-pi seams.
- [x] Create the two-parent merge commit after conflict/index audit.

### Phase 3 — Semantic seam audit
- [x] Audit external profile, ACP tracker, queue/interject/prompt, session tree/resume/fork/clone/reload.
- [x] Audit workflow, subagent V1/V2, dashboard, MCP elicitation and screen-mode switching.
- [x] Audit `.grok-pi` home/project paths, permission/trust settings and Remote TUI.
- [x] Confirm `pi-main` and adapter responsibility boundaries are unchanged.
- [x] Update source metadata/docs only after coherent integration.

### Phase 4 — Verification
- [x] `git diff --check` / conflict-marker/index audit.
- [x] `./scripts/cargo-shared.sh check -p xai-grok-pager-bin --bin grok-pi`.
- [ ] `./scripts/cargo-shared.sh test -p pi-grok-adapter`.
- [ ] `./scripts/cargo-shared.sh test -p xai-grok-pager-bin --bin grok-pi`.
- [ ] Focused Pager screen-mode/workflow/subagent/queue/product-path tests.
- [ ] `./build.sh`.
- [ ] `./verify.sh` where practical; classify pre-existing/infrastructure blockers separately.

### Phase 5 — Delivery
- [x] Update changelog/Issue with merge and verification evidence.
- [ ] ff-only verified integration tip to local `main`.
- [ ] Re-check status/graph/source identity and preserve `.pi/gapp/`.
- [ ] No remote push.

## Rollback

- Create `backup/pre-upstream-07b2f714` before the isolated merge.
- Perform all risky merge resolution only in the isolated worktree.
- Never reset/rebase/force-update main; discard a failed integration by removing only its isolated worktree/branch after preserving evidence.
- Keep the documentation record and any safety refs until final verification closes.

## Status log

- **2026-08-23:** Upstream fetched; `e5fd481..07b2f714` recorded. Static merge preview: 173 overlapping files, 49 conflicts. Architecture invariant review completed; merge not yet started.

- **2026-08-23 (integration):** Created isolated worktree `pi-grok-build-sync-07b2f714`; merged with `--no-ff --no-commit`; reviewed and resolved all predicted conflicts. Replayed only the required Pi seams: external profile/session catalog recap, Pi resource picker + upstream MCP elicitation, Pi workflow backend, queue/follow-up behavior, product-isolated paths, startup/external widget layout, timeline rail and `grok-pi` rustls provider dependency.
- **2026-08-23 (partial verification):** No unmerged entries or exact conflict markers; staged/unstaged `git diff --check` passed. `grok-pi` bin check passed. `pi-grok-adapter` unit suite passed 172/172; its subsequent rustdoc phase reported `E0463` for `xai_grok_shell` despite the same dependency compiling and linking for the library/unit suite, so final verification remains open and will be rerun once after all code is settled. Static architecture audit confirmed the adapter has no Ratatui/Crossterm dependency and `.grok-pi` startup/project routing remains present.
- **2026-08-23 (merge-ready):** Updated `AGENTS.md` upstream base to `07b2f7144fd5c5c9d3dd1966937a87852d2dbdb8`; `SOURCE_REV` is `956313d459bee15ae8f17bf73e0633605e18dddd`. Final Cargo/build verification remains explicitly open per user direction; the merge commit is being created from the conflict-free reviewed index.
- **2026-08-23 (merge commit):** Created two-parent merge commit `96ad4edf` (`981ec507` + `07b2f714`) after the index/conflict/whitespace audit. No remote action was performed.
